<!DOCTYPE html>
<html>
<head>
	<!--full_title defined in app/helpers/application_helper.rb-->
  <title><%= full_title(yield(:title)) %></title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <!--Partial defined (app/views/layouts/_shim.html.erb file created with previous HTML) so that we can use below Rails helper instead, -->
  <%= render 'layouts/shim' %>
 </head>
<body>
	<%= render 'layouts/header' %>
    <div class="container">
      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
</body>
</html>
